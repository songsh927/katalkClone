import 'package:flutter/material.dart';

class ProfileImageSetting extends StatelessWidget {
  const ProfileImageSetting({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
